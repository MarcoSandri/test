export default defineNuxtRouteMiddleware((to, from) => {
  // const localePath = useLocalePath()

  // // Redirect da tutti i link associati a '/home' o '/home-it' a '/'

  // if(to.fullPath == '/home' || to.fullPath == '/home-it') {
  //   return navigateTo(localePath('/'))
  // }
})
